// Q.3 Write a Program to evaluate the formula: (x + y)3

#include<stdio.h>

void main(){
    int x,y;
    printf("Enter the value of X + y: ");
    scanf("%d %d",&x,&y);
    printf("The value of (x+y)*3 is: %d",(x+y)*3);
}